# 项目全局约束

## 代码规范

- 代码中的注释统一使用中文。

## 文档维护

- 每完成一步操作后，都要同步更新本文件，记录已完成的操作、当前状态和后续事项。

## 操作记录

- 2026-07-15：创建项目全局约束文档，明确中文注释与逐步更新文档的要求。
- 2026-07-15：完成 RAG 现状盘点，确认当前后端未接入会话记忆；制定 `docs/rag-upgrade-plan.md` 分阶段升级路线；实现阶段 1 的服务端会话记忆闭环及对应回归测试。Python 语法编译、前端构建及 RAG 记忆/接口/检索回归测试均已通过（9 passed）。
- 2026-07-15：根据 RAG 优化模块图，完成知识工程、Query 改写、检索召回、Rerank、截断、可信生成、过程评测和反馈闭环的代码映射与实施方案，并更新 `docs/rag-upgrade-plan.md`。
- 2026-07-15：完成 RAG 第 0～1 步：新增可版本化金标集模板和加载器；修正按文档类型选择分块预设；为上传索引增加质量报告；新增回归测试。RAG 相关测试已通过（29 passed）。
- 2026-07-15：开始 RAG 第 2 步：新增 QueryPlan 规则层与受控配置；为 pgvector 检索增加文件名、页码、Sheet、文件类型的参数化过滤和索引。下一步接入多路召回、内部 Trace 与回归测试。
- 2026-07-15：完成 RAG 第 2 步主检索链路：普通问题零模型调用；追问或弱信号问题按需生成最多两条变体；多路结果以加权 RRF 合并并限制单文档候选数；仅记录内部结构化 Trace。下一步补齐真实金标集、测试和文档同步。
- 2026-07-15：修复 QueryPlan 接入后的兼容性：知识库不可用异常保持原有语义，流式回归测试改为替换新的 QueryPlan 检索入口。下一步继续补齐专项测试与真实金标集。
- 2026-07-15：补齐 QueryPlan 专项测试，覆盖显式过滤、普通问题零模型调用、追问改写、非法模型输出回退、参数化 SQL、多路 RRF、单文档配额及候选不外泄；新增基于当前 PDF、Excel、Word 文档的 32 条金标集。下一步同步架构/API/升级文档并执行完整验证。
- 2026-07-15：修正 QueryPlan 文件名解析：去除中文书名号，并避免把文件扩展名误识别为独立文件类型条件。下一步重新执行完整回归并同步文档。
- 2026-07-15：完成 RAG 第 2 步文档与配置同步：更新 QueryPlan 数据流、SSE 证据边界、内部 Trace、元数据过滤和三层记忆完成状态；补充环境变量示例。下一步执行完整后端测试、前端构建与真实金标集评测。
- 2026-07-15：完整后端测试通过（39 passed）；真实金标集已校验为 34 条、含 2 条拒答。当前本机 PostgreSQL 5432 端口未监听，真实检索指标评测待启动 pgvector 并完成文档索引后执行；下一步完成前端构建与最终静态检查。
- 2026-07-15：前端生产构建通过；复核并保持非流式 `retrieved_count` 的旧语义，仅返回最终切片数量、不泄漏 QueryPlan 内部候选数量。下一步执行最终后端回归与静态检查。
- 2026-07-15：RAG 第 2 步实施完成并通过最终验证：Python 全量测试 40 passed、前端 `npm run build` 通过、Python 编译与 `git diff --check` 通过。当前待办仅为启动 PostgreSQL + pgvector、重新索引 3 份文档后，运行 34 条真实金标集并记录 Recall@5、MRR、NDCG@5 基线对比。
- 2026-07-15：pgvector 已连通，确认已索引 3 份文档、62 个切片；发现原评测器仅测旧 hybrid 且会把同文档重复切片计入文档级指标。已新增 QueryPlan 评测策略和文档去重指标修正，下一步运行真实基线与 QueryPlan 对比。
- 2026-07-15：首次真实对比发现 QueryPlan 漏掉 2 条文件名过滤样本（指标 0.938）；根因是文件名主体/中文方括号与严格等值条件不匹配。已改为保留文件名原字符、精确匹配优先并以转义的包含匹配兜底，同时增加文件名 trigram 索引。下一步回归并重跑真实评测。
- 2026-07-15：定位并继续修复 Excel 文件名过滤：上传文件含前后缀时，完整名包含匹配仍会失败；已补充文件 stem 的转义包含匹配，仍保留精确匹配和参数化约束。下一步验证两个漏召回样本并重跑真实评测。
- 2026-07-15：完成真实 QueryPlan 验收：pgvector 中 3 份文档、62 个切片；34 条金标中 32 条可检索样本的 hybrid 与 QueryPlan 的 Recall@5、MRR、NDCG@5 均为 1.000，QueryPlan 平均检索延迟 303.0ms，较 hybrid 的 580.4ms 降低约 48%。已同步升级计划与架构文档，下一步执行最终全量回归。
- 2026-07-15：RAG 第 2 步与真实评测闭环完成：Python 全量回归 44 passed、语法编译与 `git diff --check` 通过；前端生产构建已在本轮前通过。当前下一步为第 3 步：离线验证真实 Cross-Encoder Rerank 的收益与延迟，再决定是否开启线上重排。
- 2026-07-15：已暂存本次 RAG 升级相关的 35 个文件（检索、记忆、评测、RAG 接口、前端页面、文档与测试）；认证、部署脚本与运行数据等无关未跟踪文件保持未暂存，待用户另行确认。
- 2026-07-15：完成 RAG 三层记忆优化：短期会话、抽取式长期摘要、用户显式偏好；接入 Redis 可选缓存与用户偏好管理 API。RAG 相关后端测试通过（32 passed），前端构建通过。
- 2026-07-15：开始 RAG 第 3 步的真实 Cross-Encoder Rerank 实施：新增本地模型加载、批量打分、候选数/字符数/超时保护和 RRF 回退；接入离线 QueryPlan Rerank 策略、诊断及可选启动预热。下一步补齐专项回归并运行真实金标对比决定线上开关。
- 2026-07-15：补齐 Rerank 专项回归：验证模型分数排序、元数据保留、默认不调用、模型不可用时 RRF 回退，以及 QueryPlan Rerank 评测策略注册；非流式接口在显式线上开关开启时也会应用受控 Rerank。下一步运行真实离线金标对比。
- 2026-07-15：完成 RAG 第 3 步真实 Rerank 验证：Cross-Encoder 专项测试通过（19 passed）；32 条可检索金标样本中 QueryPlan 与 QueryPlan Rerank 的 Recall@5、MRR、NDCG@5 均为 1.000，但 Rerank 平均延迟 1434.1ms 高于基线 800.9ms。因此不满足上线收益门槛，`RAG_ONLINE_RERANK` 保持关闭；下一步进入 ContextBuilder 与可信生成闸门。
- 2026-07-15：开始 RAG 第 4 步 ContextBuilder：新增全局 token 预算、切片去重、同文档配额、按得分分配和相邻块拼接；生成链路仅使用真正进入上下文的证据，避免未入模候选被引用。下一步补齐 ContextBuilder/可信生成测试并接入充分性与引用覆盖闸门。
- 2026-07-15：补齐 ContextBuilder 的预算、同文档配额、重复切片、相邻拼接与 Rerank 分数优先测试，并修正候选数过多时的全局预算边界。下一步实现可信生成的证据充分性和引用覆盖闸门。
- 2026-07-15：修正 ContextBuilder 截断边界：将省略号本身计入分配预算，避免极端预算下误丢弃后一条证据。下一步重跑 ContextBuilder 回归并实现可信生成闸门。
- 2026-07-15：开始可信生成闸门：新增证据最小充分性、逐实质性结论的合法引用覆盖校验和一次受控重生成；证据不足或重生成仍不合格时固定拒答，且不返回未支撑来源。下一步补齐可信生成回归与端到端验证。
- 2026-07-15：补齐可信生成规则回归：覆盖短证据拒答、逐句合法引用覆盖与非法引用拒绝。下一步运行 ContextBuilder、可信生成、流式引用的组合回归并同步设计文档。
- 2026-07-15：修正可信生成引用切句：支持“句号后引用”的常见写法，并将其归属到前一结论句；未引用结论仍会被拒绝。下一步重新运行组合回归。
- 2026-07-15：ContextBuilder 与可信生成组合回归通过（28 passed）；已同步第 4 步预算、证据边界、拒答与引用覆盖设计。下一步使用已索引知识库执行真实有答案/无答案问答验证，再进入过程 Trace 与反馈闭环。
- 2026-07-15：完成可信生成真实问答验证并收紧无关召回保护：证据除最小长度外还须命中有效问题词；“老板生日”类无依据问题固定拒答且不返回来源。引用校验连续失败时只降级展示可核查来源、不输出事实结论，避免把有证据的正常问题误拒。可信生成相关组合回归通过（29 passed）。下一步实现全过程 Trace、答案级评测与反馈处理队列。
- 2026-07-15：完成第 5 步 Trace 与反馈队列首段：每次 RAG 查询写入脱敏 Trace（问题指纹、规划摘要、候选数、最终引用、结果模式、耗时），不记录原始问题或未采纳候选，也不改变 SSE；差评自动归因为知识缺口、回答质量或引用错误并进入 `pending` 队列。专项回归通过（21 passed），真实拒答 Trace 已验证不含原始问题且无采纳引用。下一步补充答案级离线评测并执行完整回归。
- 2026-07-15：完成本轮第 5 步验证：后端全量测试通过（55 passed），前端 `npm run build`、Python 编译和 `git diff --check` 均通过；运行时生成的 `data/rag-traces.jsonl` 保持为未跟踪数据，不纳入代码提交。下一步为金标集补充审核后的答案级预期与引用期望，建立拒答正确率、引用覆盖率和结论支持率的离线评测。
- 2026-07-16：推进第 5 步答案级评测：为 32 条正例补充关键事实词金标、拒答/引用契约和确定性评测器；修复版本号小数点误切句、预测销量被历史数据误答、Excel 精确 ID 召回及跨分块条款证据缺失。真实 34 条评测达到拒答正确率、引用覆盖率、引用合法率均为 1.000，关键事实词覆盖率 0.818、结论-证据支持率 0.719、平均延迟 1941ms；专项回归通过（21 passed）。下一步将答案词改为同义词组并人工复核剩余多步骤/同义表达样本。
- 2026-07-16：完成答案金标同义词组与第二轮真实评测：评测器允许审核过的等价表达，相关回归通过（28 passed）。34 条真实样本的拒答正确率、引用覆盖率和引用合法率均为 1.000；关键事实词覆盖率升至 0.880、结论-证据支持率升至 0.844、平均延迟降至 1594ms。剩余 5 条失败集中于年假表格跨块和评教流程表达，下一步逐条判定是检索缺口还是补充受审核同义词组。
- 2026-07-16：完成本轮答案级评测全量验证：Python 全量测试通过（63 passed），Python 编译和 `git diff --check` 通过。剩余失败已分流：离职/评教为金标完整性复核，满 20 年、入职 15 天、部门 30% 为“候选数足够但目标条款未入模”的检索低置信问题；下一步增强低置信检测和受控关键词召回，不放宽引用或拒答规则。
- 2026-07-16：实现低置信检索触发：长中文问题除候选数阈值外，还检查候选对关键语义词的实际支撑；信号弱时才进入既有受控 QueryPlan 改写，短普通问题保持零模型调用。修正中文分词整句化导致的弱信号漏判，专项回归通过（18 passed）。下一步用真实年假条款样本复测受控改写收益。
- 2026-07-16：修复 PDF 知识工程解析质量：自动模式下当 MinerU 漏掉至少两条结构化年限行或三个数值事实时，保留其正文并补入同页 PyPDF 文本；专项回归通过（22 passed）。已重新索引年假 PDF（替换 10 个旧切片为 6 个新切片），pgvector 已确认包含 `5天/10天/15天/入职15日`；满 20 年问题已恢复为 15 天带页码引用答案。下一步继续处理同义条款被部分相关 FAQ 遮蔽的低置信改写触发。
- 2026-07-16：增加零模型领域同义规则变体，并修正简单字符串替换造成的病句：新员工工龄材料和部门同时休假上限均使用完整、可审计的检索表述；不影响普通问题的零模型路径。QueryPlan 专项回归通过（14 passed）。下一步用修正后的完整变体复测对应条款召回。
- 2026-07-16：完成跨相邻切片条款的证据扩展验证：检索命中切片会受控补入相邻证据并按问题词命中排序；“新员工入职多少天内提交累计工龄证明”已稳定回答为“入职15日内”，且仅引用年假制度第 2 页。下一步运行 34 条真实答案级金标集，确认整体质量与延迟。
- 2026-07-16：修正相邻证据排序：主命中与相邻块统一使用稳定中文双字词项评分，相邻块不再叠加父切片的词项分；非流式与流式链路均使用 QueryPlan 的受控变体补齐相邻证据。文档编号、入职 15 日、部门 30% 三项真实验证均正确，ContextBuilder 与 QueryPlan 专项回归通过（19 passed）。下一步重跑 34 条真实答案级金标集。
- 2026-07-16：完成 34 条真实答案级金标的分批复测：拒答正确率、引用覆盖率、引用合法率、关键事实词覆盖率和结论-证据支持率均为 1.000；分批冷启动加权平均端到端延迟为 3914.6ms，不能直接与单进程延迟基线比较。人工复核并收紧“评教第一步”金标，仅要求新版教务系统与登录。ContextBuilder、QueryPlan 与答案评测专项回归通过（22 passed）。下一步补充单进程持续评测与 P50/P95 延迟统计，并执行全量回归。
- 2026-07-16：本轮全量验证完成：后端 Python 测试 67 passed，前端 `npm run build` 通过，Python 编译和 `git diff --check` 通过；依赖中的 FastAPI TestClient 与 Redis `setex` 有已知弃用警告，未影响本轮功能。下一步实现单进程持续答案评测与 P50/P95 延迟统计，形成可比较的性能基线。
- 2026-07-16：增强答案级评测器：单进程运行可输出脱敏样本进度、P50/P95 延迟与可选 JSON 报告，避免长评测只能依赖分批冷启动统计；答案评测专项测试通过（3 passed）。下一步运行完整单进程评测并记录新的性能基线。
- 2026-07-16：完成评测器与文档的最终验证：后端 Python 测试 67 passed，前端 `npm run build` 通过，Python 编译与 `git diff --check` 通过。单进程全量评测受桌面单条命令 60 秒等待上限限制，已提供可在用户终端持续运行的命令和 P50/P95 JSON 报告；当前有效质量基线仍为分批真实 34 条评测的五项指标均 1.000。下一步用用户终端运行单进程命令，记录可比较的 P50/P95 性能基线。
- 2026-07-16：补齐管理员反馈闭环：差评入队后可按待处理/已结案查看，管理员可将其归档到金标集、知识工程、检索调优或忽略，并以只追加审计动作保存处理人、时间和说明；不会自动改写金标或知识库。反馈专项回归通过（4 passed）。下一步运行全量回归、前端构建与静态检查，并继续补单进程 P50/P95 性能基线。
- 2026-07-16：管理员反馈闭环完成验证：后端 Python 测试 69 passed，前端 `npm run build` 通过，Python 编译与 `git diff --check` 通过。下一步基于既有脱敏 Trace 输出运行态 P50/P95，避免性能诊断只显示静态目标值。
- 2026-07-16：补齐运行态性能观测：`/api/rag/diagnostics` 现在汇总最近 100 条脱敏 Trace 的样本数、答案模式分布和检索/生成/总耗时 P50/P95，不暴露问题、答案或候选正文；Trace 专项回归通过（5 passed）。下一步运行全量回归、前端构建与静态检查。
- 2026-07-16：运行态性能观测完成验证：后端 Python 测试 70 passed，前端 `npm run build` 通过，Python 编译与 `git diff --check` 通过。下一步检查管理员前端入口，将反馈队列的人工结案能力接入可操作页面。
- 2026-07-16：接入管理员反馈运营页面：仅 `admin` 角色可见 `/feedback` 导航与页面，可查看待处理/已结案反馈、当时回答和已采纳来源，并选择后续动作、填写说明后人工结案；路由与后端 API 均保留权限校验。前端生产构建通过。下一步运行全量后端回归、前端构建与静态检查。
- 2026-07-16：反馈运营页面完成验证：后端 Python 测试 70 passed，前端 `npm run build` 通过，Python 编译与 `git diff --check` 通过。下一步检查运行中服务的诊断接口，确认真实 Trace 能输出运行态 P50/P95。
- 2026-07-16：完成真实运行态验证：`/api/health` 正常，`/api/rag/diagnostics` 可读取 58 个切片和脱敏 Trace；UTF-8 客户端下文档编号问答正确，检索 192.4ms、生成 714.7ms、总计 907.2ms。最近热态 Trace 的总耗时 P50 为 1142.2ms、P95 为 3651.5ms，P95 尚未达到目标；完整远程金标评测受桌面单条命令 60 秒限制中断，服务保持健康。下一步为 QueryPlan 增加受控超时与回退，压缩检索长尾。
- 2026-07-16：修正运行态延迟口径：Trace 增加每次应用启动的 `runtime_id`，`/api/rag/diagnostics` 仅汇总当前运行批次，避免历史冷启动/压测污染热态 P50/P95。真实服务中首条重载后请求为 4079.7ms（预热），第二条热态请求为 851.1ms（检索 95.0ms、生成 755.9ms），两次回答均正确引用年假制度首页。下一步运行全量回归、前端构建与静态检查。
- 2026-07-16：本轮运行态隔离与反馈运营能力完成最终验证：后端 Python 测试 70 passed，前端 `npm run build` 通过，Python 编译与 `git diff --check` 通过；真实服务健康检查、当前运行批次 Trace 与 UTF-8 问答均已验证。下一步需在持续运行的服务中积累更多当前批次样本并以 P50/P95 设定告警基线，同时随新增真实文档扩充金标集。
- 2026-07-16：补齐反馈队列基础 SLA：管理员接口和 `/feedback` 页面现展示待处理数、已结案数、待办归因分布与最久等待时长；所有数据由已有脱敏归因和时间戳聚合，不额外存储用户内容。反馈专项测试 5 passed、前端生产构建通过。下一步运行全量后端回归、前端构建与静态检查。
- 2026-07-16：反馈 SLA 完成最终验证：后端 Python 测试 70 passed，前端 `npm run build` 通过，Python 编译与 `git diff --check` 通过。下一步继续扩充真实金标与长时间运行样本，为反馈 SLA 和当前批次 P50/P95 设置正式告警阈值。
- 2026-07-16：补齐知识工程版本可见性：上传质量报告中的 SHA-256 与索引时间会持久化到全部切片，文档列表在服务重启后仍可返回 `version`，前端展示版本 SHA 前缀以确认同文件名重传已覆盖旧版本。知识工程专项测试 6 passed、前端生产构建通过。下一步运行全量后端回归、前端构建与静态检查。
- 2026-07-16：知识工程版本可见性完成最终验证：后端 Python 测试 71 passed，前端 `npm run build` 通过，Python 编译与 `git diff --check` 通过。下一步核对现有已索引文档并在无内容变更前提下重索引，使当前知识库也获得持久化版本元数据。
- 2026-07-16：修正知识库文档列表：忽略仓库占位文件 `.gitkeep`，避免其被误展示为待索引文档；补充 API 回归测试。全量后端测试 72 passed、前端 `npm run build`、Python 编译与 `git diff --check` 均通过。当前运行中的服务尚未加载新版索引代码，需重启后对现有 3 份文档执行一次重索引，才能把版本哈希和索引时间写入已有切片并完成线上验证。
- 2026-07-16：完成线上迁移前排查：8000 端口服务健康但仍返回 `.gitkeep` 且无 `version`，确认实际 Uvicorn 父/子进程启动早于本轮代码修改，自动重载未生效；未对用户运行中的进程执行强制停止。下一步由用户完整重启服务后，重索引 3 份既有文档并核验 pgvector 中的版本元数据。
- 2026-07-16：补齐受控文档日期治理：上传可选接收人工确认的 `document_date`，以 ISO 日期随全部切片持久化并在文档列表显示；QueryPlan 只解析用户明确日期并以参数化 SQL 过滤该字段，不从文件名、文件时间或模型猜测日期。日期解析、SQL、索引元数据和 API 回归测试 26 passed。下一步运行全量后端回归、前端构建与静态检查；服务重启后为既有文档补齐版本元数据。
- 2026-07-16：受控文档日期治理完成最终验证：后端 Python 测试 74 passed，前端 `npm run build` 通过，Python 编译与 `git diff --check` 通过。当前线上服务仍未加载本轮版本/日期代码；下一步完整重启服务，为 3 份既有文档重索引以写入版本元数据，并仅在人工确认后补填其文档日期。
- 2026-07-16：补齐反馈负责人和 SLA 治理：管理员可分派负责人，队列汇总默认 72 小时 SLA 的逾期数和未分派数，分派与结案均使用只追加审计动作，旧结案记录保持兼容；`/feedback` 页面可直接操作。反馈专项测试 6 passed。下一步运行全量后端回归、前端构建与静态检查；线上服务重启后继续完成既有文档的版本迁移。
- 2026-07-16：反馈负责人和 SLA 治理完成最终验证：后端 Python 测试 75 passed，前端 `npm run build` 通过，Python 编译与 `git diff --check` 通过。`RAG_FEEDBACK_SLA_HOURS` 默认 72 小时且可配置；正式告警阈值仍需依据生产运行样本设定。当前下一步为完整重启 8000 服务，再为既有 3 份文档重索引并核验版本元数据。
- 2026-07-16：再次核验线上迁移前提：8000 服务进程仍为 10:36 启动的旧运行批次，`/api/rag/documents` 仍显示 `.gitkeep` 且所有文档无 `version`；当前不能在旧代码上安全执行重索引迁移。待用户完整重启服务后继续。
- 2026-07-16：完成线上知识工程迁移与验收：使用项目 `.venv` 启动新版服务后，3 份既有文档已全部重索引（Word 1、Excel 101、PDF 6，共 108 个切片）；文档列表已隐藏 `.gitkeep` 并为每份文档返回持久化 SHA-256 与索引时间。真实年假问答正确且仅返回实际引用；32 条可检索金标的 QueryPlan Recall@5、MRR、NDCG@5、Hit Rate@5 均为 1.000，平均检索延迟 1110ms（含冷启动和按需改写）。下一步继续基于真实新增文档扩充金标，并依据运行态 Trace 样本设定正式延迟与反馈告警阈值。
- 2026-07-16：完成单进程答案级基线与回归修复：发现“年假申请审批顺序”口语同义未覆盖会误召回 Excel，新增零模型“年假→年休假”检索变体并通过专项测试；人工复核金标后移除不属于审批顺序的 OA 提交入口。重跑 34 条真实样本，拒答正确率、引用覆盖率、引用合法率、关键事实词覆盖率、结论-证据支持率均为 1.000，平均延迟 1688.7ms、P50 1122.6ms、P95 2810.7ms。下一步运行全量回归、前端构建与静态检查，并在持续生产样本上设置正式告警阈值。
- 2026-07-16：单进程答案级基线与口语同义回归修复完成最终验证：后端 Python 测试 76 passed，前端 `npm run build` 通过，Python 编译与 `git diff --check` 通过。当前 34 条金标的五项质量指标均为 1.000，P50/P95 已记录；下一步基于持续生产 Trace 与新增真实文档扩充金标，并将 P95/SLA 的告警阈值从默认值调整为运营确认值。
- 2026-07-16：补齐运行态 P95 告警：`/api/rag/diagnostics` 新增可配置总耗时 P95 阈值（默认 3000ms）和最小样本数（默认 20），在样本不足、正常和超阈值间明确区分；真实热重载后的新运行批次返回 `insufficient_samples`，避免预热误告警。告警专项测试 7 passed。下一步运行全量回归、前端构建与静态检查。
- 2026-07-16：运行态 P95 告警完成最终验证：后端 Python 测试 77 passed，前端 `npm run build` 通过，Python 编译与 `git diff --check` 通过。当前新运行批次尚未积累 20 条 Trace，`latency_alert` 保持 `insufficient_samples`；下一步在真实使用中积累当前批次样本、观测告警状态，并随新增文档扩充经人工审核的金标集。
- 2026-07-16：补齐 RAG 生产运行手册：明确人工日期入库、版本核验、发布前检索/答案评测、当前批次 P95 告警处理、管理员反馈分派/结案和新增文档的金标扩容规则。下一步依据真实使用 Trace 和新增业务文档执行手册中的持续运营动作。
- 2026-07-16：完成升级方案状态核对：第 0～6 步代码能力均已完成并通过验证；线上已有 3 份文档、108 个切片，34 条单进程金标的五项质量指标均为 1.000。当前 `/diagnostics` 的新运行批次尚无 Trace，因此告警状态为 `insufficient_samples`；后续为真实运营数据与人工审核任务，不是未实现功能。已修正升级方案开头的旧缺口表述。
- 2026-07-16：完成启动日志排查：`main.py` 的根日志已配置为 `stdout` 且 `LOG_LEVEL=INFO`，代码未拦截终端日志；发现存在两组 Uvicorn 进程，其中 12:32 的一组由隐藏后台启动，8000 当前实际监听者为 13:31 启动的 PID 13520。未未经用户确认停止任何进程；下一步应只保留一组在可见终端启动的 Uvicorn，以恢复可见访问日志。
